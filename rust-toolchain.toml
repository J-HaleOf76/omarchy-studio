# Pin the toolchain so local dev and CI lint/format identically — this file
# is what keeps `clippy -D warnings` from breaking on a surprise version bump.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
